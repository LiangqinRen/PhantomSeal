defaults:
  - evaluate: evaluate
  - third_party: simswap
  - _self_

log_suffix: ""
image_dir: log/${now:%Y-%m-%d}/${now:%H:%M:%S}${log_suffix}/image
random_seed: 0

hydra:
  run:
    dir: log/${now:%Y-%m-%d}/${now:%H:%M:%S}${log_suffix}