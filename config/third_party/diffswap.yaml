name: diffswap
function: metric

# origin:

dataset:
  metric_dir: data/FFHQ_512
  metric_pairs: 3000
  cloak: real
  cloak_dir: "data/FFHQ_cloak/${third_party.dataset.cloak}"
  cloak_mix: true
  cloak_index: 0