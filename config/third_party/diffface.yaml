name: diffface
function: metric

# origin:

dataset:
  resolution: 1024
  metric_dir: data/FFHQ_${third_party.dataset.resolution}
  metric_pairs: 3000
  input_size: 256
  cloak: real
  cloak_dir: "data/FFHQ_cloak/${third_party.dataset.cloak}"
  cloak_mix: true
  cloak_index: 0

defense:
  batch_size: 1 # limited by DiffFace